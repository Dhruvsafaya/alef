{
    env: "test"
    execution_env: "local-test"
    s3_endpoint: "http://localhost:9000"

    s3.path: "s3://test-bucket/data"
    delta.snapshot.partitions: 5

    influxdb.cred: "nprd/influxdb"
    redshift.urlkey: "nprd/redshift"
    redshift.database: "bigdatadb"
    redshift.sqlexceptiontimeout: 0
    redshift.sqlexceptionattempts: 3
    serviceDeskSecretName: "nprd/service_desk"

    redshift-connection-timeout-min: 5

    s3.coalesce: 2

   internal: {
     batch.execution.log.table: ""${?env}"._internal.batch_execution_log"
   }
    resource1: {
        one: "one1",
        two: "two2"
    }

    redshift-staging-schema: "alefdw_stage"
    redshift-schema: "alefdw"

    sink-one: {name: "sink-one-value"}
    sink-two: {name: "sink-two-value"}

    service-name {
        sink: [
            "sink-one",
            "sink-two"
        ]
        exclude-columns: ["one", "two", "three"]
    }
}
